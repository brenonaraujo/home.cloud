# apps/identity

Identidade as-code (IdP). Grupos, applications, bindings.

**Status:** não neste git ainda. Código vivo em `~/Projects/brenon-cloud-identity`. Move na Fase 5.

Regra: um app novo = apply só desse slug. Não reescrever todos os client secrets.
